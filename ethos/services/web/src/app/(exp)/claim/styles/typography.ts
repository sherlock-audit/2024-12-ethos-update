import { css } from '@emotion/react';

export const claimTitle = css({
  fontSize: 36,
  zIndex: 1,
  textAlign: 'center',
});

export const claimDescription = css({
  fontSize: 22,
  lineHeight: 1.5,
  textAlign: 'center',
  zIndex: 1,
});
